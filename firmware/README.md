# Firmware

Placeholder for the hardware engineering stage (stage 2, after phases A–D of
the research roadmap — see `docs/roadmap.md`).

Planned scope once this stage starts: MyoWare EMG + IMU sensor firmware and an
Arduino-driven actuator, implementing `reborn.hal.hardware_backend` so the
existing sensing/decision/safety/control code runs unchanged against real
hardware (see `docs/architecture.md`, "HAL — the spine of the codebase").

Not started yet.
