# References

Zotero library export for this review lives here (RIS or BibTeX), e.g. `adherence-review.bib`.

- Commit **only** the exported bibliography and any notes — not Zotero's internal storage or PDFs.
- Keep the export in sync with the Zotero collection; re-export when the included set changes.
- The paper cites this bibliography; the bibliography's git version is part of the reproducibility
  record (protocol → search → screen → charted studies → references, all versioned together).
